services:
  - type: web
    name: multi-agent
    runtime: python
    plan: free
    buildCommand: pip install -r requirements-agency.txt
    startCommand: python -m uvicorn server:app --host 0.0.0.0 --port $PORT

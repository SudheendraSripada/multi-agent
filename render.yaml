services:
  - type: web
    name: multi-agent
    runtime: python
    plan: free
    buildCommand: python3 -m venv .venv && .venv/bin/pip install --upgrade pip && .venv/bin/pip install -r requirements-agency.txt
    startCommand: bash start.sh
